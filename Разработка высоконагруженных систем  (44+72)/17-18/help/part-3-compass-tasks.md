# Задания для MongoDB Compass

## 🎯 Практические задания в графическом интерфейсе

### Подготовка
1. Открыть MongoDB Compass
2. Подключиться: `mongodb://student:password@localhost:27017/`
3. Создать базу данных `интернет_магазин`

## Задание 1: Создание коллекции заказов

### Шаг 1: Создать коллекцию
- Database Name: `интернет_магазин`
- Collection Name: `заказы`

### Шаг 2: Добавить документы
Нажать **INSERT DOCUMENT** и добавить:

**Заказ 1:**
```json
{
  "номерЗаказа": 1001,
  "клиент": "Иван Петров",
  "товары": [
    {"товар": "Ноутбук", "количество": 1, "цена": 50000},
    {"товар": "Мышь", "количество": 2, "цена": 1500}
  ],
  "сумма": 53000,
  "статус": "доставлен",
  "дата": {"$date": "2024-12-02T10:00:00Z"}
}
```

**Заказ 2:**
```json
{
  "номерЗаказа": 1002,
  "клиент": "Мария Сидорова",
  "товары": [
    {"товар": "Телефон", "количество": 1, "цена": 30000}
  ],
  "сумма": 30000,
  "статус": "ожидает",
  "дата": {"$date": "2024-12-02T11:00:00Z"}
}
```

**Заказ 3:**
```json
{
  "номерЗаказа": 1003,
  "клиент": "Петр Иванов",
  "товары": [
    {"товар": "Планшет", "количество": 1, "цена": 25000},
    {"товар": "Клавиатура", "количество": 1, "цена": 3000}
  ],
  "сумма": 28000,
  "статус": "обработка",
  "дата": {"$date": "2024-12-02T12:00:00Z"}
}
```

## Задание 2: Поиск через Filter

### Задача 1: Найти заказы клиента
**Filter:** `{"клиент": "Иван Петров"}`

### Задача 2: Найти заказы по статусу
**Filter:** `{"статус": "ожидает"}`

### Задача 3: Найти дорогие заказы
**Filter:** `{"сумма": {"$gt": 40000}}`

### Задача 4: Найти заказы с конкретным товаром
**Filter:** `{"товары.товар": "Ноутбук"}`

### Задача 5: Найти заказы за период
**Filter:** `{"дата": {"$gte": {"$date": "2024-12-02T10:00:00Z"}}}`

## Задание 3: Обновление документов

### Задача 1: Изменить статус заказа
1. **Filter:** `{"номерЗаказа": 1002}`
2. Найти документ → **Edit Document**
3. Изменить: `"статус": "отправлен"`
4. Нажать **UPDATE**

### Задача 2: Добавить поле доставки
1. **Filter:** `{"статус": "доставлен"}`
2. **Edit Document** → добавить:
   ```json
   "датаДоставки": {"$date": "2024-12-01T15:00:00Z"}
   ```

### Задача 3: Обновить цену товара
1. Найти заказ с "Мышь"
2. Изменить цену мыши на 1700

## Задание 4: Агрегация данных

### Перейти на вкладку **Aggregations**

### Задача 1: Группировка по статусам
```json
[
  {
    "$group": {
      "_id": "$статус",
      "количество": {"$sum": 1},
      "общаяСумма": {"$sum": "$сумма"}
    }
  }
]
```

### Задача 2: Средняя сумма заказа
```json
[
  {
    "$group": {
      "_id": null,
      "средняяСумма": {"$avg": "$сумма"},
      "максСумма": {"$max": "$сумма"},
      "минСумма": {"$min": "$сумма"}
    }
  }
]
```

### Задача 3: Топ клиентов по сумме
```json
[
  {
    "$group": {
      "_id": "$клиент",
      "общаяСумма": {"$sum": "$сумма"},
      "количествоЗаказов": {"$sum": 1}
    }
  },
  {
    "$sort": {"общаяСумма": -1}
  }
]
```

### Задача 4: Анализ товаров
```json
[
  {"$unwind": "$товары"},
  {
    "$group": {
      "_id": "$товары.товар",
      "общееКоличество": {"$sum": "$товары.количество"},
      "общаяВыручка": {"$sum": {"$multiply": ["$товары.количество", "$товары.цена"]}}
    }
  },
  {"$sort": {"общаяВыручка": -1}}
]
```

## Задание 5: Работа с индексами

### Перейти на вкладку **Indexes**

### Задача 1: Создать индекс по клиенту
- **CREATE INDEX**
- Fields: `{"клиент": 1}`
- Index Name: `индекс_клиент`

### Задача 2: Составной индекс
- Fields: `{"статус": 1, "сумма": -1}`
- Index Name: `индекс_статус_сумма`

### Задача 3: Текстовый индекс
- Fields: `{"клиент": "text", "товары.товар": "text"}`
- Index Name: `текстовый_поиск`

## Задание 6: Схема данных

### Перейти на вкладку **Schema**

### Задача 1: Анализ структуры
1. Изучить распределение типов данных
2. Посмотреть уникальные значения в поле "статус"
3. Проанализировать диапазон сумм заказов

### Задача 2: Валидация схемы
Создать правила валидации для коллекции:
```json
{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["номерЗаказа", "клиент", "сумма", "статус"],
    "properties": {
      "номерЗаказа": {"bsonType": "int"},
      "клиент": {"bsonType": "string"},
      "сумма": {"bsonType": "int", "minimum": 0},
      "статус": {"enum": ["ожидает", "обработка", "отправлен", "доставлен"]}
    }
  }
}
```

## Задание 7: Экспорт и импорт

### Экспорт данных
1. Выбрать коллекцию `заказы`
2. **Collection** → **Export Collection**
3. Формат: JSON
4. Сохранить файл

### Импорт данных
1. Создать новую коллекцию `заказы_копия`
2. **Collection** → **Import Data**
3. Выбрать сохраненный файл
4. Проверить импорт

## 🏆 Дополнительные задания

### Задание 8: Создать коллекцию товаров
```json
{
  "артикул": "NB001",
  "название": "Ноутбук Gaming",
  "категория": "Электроника",
  "цена": 75000,
  "наличие": 15,
  "характеристики": {
    "процессор": "Intel i7",
    "память": "16GB",
    "диск": "512GB SSD"
  }
}
```

### Задание 9: Связать заказы с товарами
Создать запрос для поиска заказов с детальной информацией о товарах.

### Задание 10: Создать дашборд
Использовать агрегации для создания сводки:
- Общее количество заказов
- Общая выручка
- Средний чек
- Топ-3 товара по продажам