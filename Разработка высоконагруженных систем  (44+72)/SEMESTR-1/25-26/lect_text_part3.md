# Текст лекции: PostgreSQL Full-Text Search (Часть 3)

## Слайд 21: Индекс RUM

**Время: 5 мин**

**Ассоциация:** RUM — это GIN на стероидах (как Postgres Pro по сравнению с обычным Postgres).

**RUM (RUM Index) — расширение на основе GIN**

**Что добавляет к GIN:**
- 📍 **Хранит позиции лексем** в документе
- ⚖️ **Хранит веса** (A, B, C, D)
- 🎯 **Оператор `<=>`** для сортировки по релевантности БЕЗ вычислений

**Магия:**

```sql
-- GIN: сначала найти, потом сортировать в памяти
SELECT * FROM docs 
WHERE fts @@ query 
ORDER BY ts_rank(fts, query) DESC;
-- Seq Scan + Sort

-- RUM: сортировка прямо по индексу!
SELECT * FROM docs 
ORDER BY fts <=> query;
-- Index Scan using rum_idx (cost=...)
```

**Что происходит с GIN:**
1. Находим все документы (Index Scan)
2. Для каждого вычисляем ts_rank (CPU)
3. Сортируем в памяти (Sort)

**Что происходит с RUM:**
1. Index Scan сразу возвращает документы в порядке релевантности
2. Всё!

**Почему это возможно?**

RUM хранит дополнительную информацию в индексе:
- Позиции лексем (для фразового поиска)
- Веса лексем (для ранжирования)

Это позволяет вычислить релевантность прямо при обходе индекса.

**Минусы:**
- Больше места на диске (хранит больше данных)
- Медленнее INSERT/UPDATE (больше данных для обновления)
- Нужно устанавливать расширение (не из коробки)

**Когда использовать:**
- Нужна сортировка по релевантности (поисковики, рекомендации)
- Фразовый поиск критичен
- Готовы пожертвовать местом ради скорости

**Сленг:** "Рам индекс" — RUM индекс.

---

## Слайд 22: Выбор индекса — cheat sheet

**Время: 3 мин**

**Ассоциация:** Выбор индекса — это как выбор между HashMap, TreeMap и LinkedHashMap.

**Cheat sheet:**

- **Много записей?** → GIN
- **Частые обновления?** → GiST
- **Нужна сортировка по релевантности?** → RUM
- **Фразовый поиск критичен?** → RUM

**Золотое правило:** Размер шарда = 10-50 ГБ (это для Elasticsearch, но принцип тот же).

**Для большинства случаев:** GIN с fastupdate=on.

**Сленг:** "Выбрать правильный индекс" — половина успеха в оптимизации поиска.

---

## Слайд 23: Создание индекса GIN

**Время: 5 мин**

**Ассоциация:** Индекс — это как кэш, но на диске.

Окей, практика. Создаем индекс.

```sql
-- Создание индекса
CREATE INDEX idx_fts ON articles 
USING GIN (to_tsvector('russian', content));

-- Использование индекса
SELECT * FROM articles 
WHERE to_tsvector('russian', content) @@ to_tsquery('postgres');
```

**ВАЖНО:** Выражение в индексе должно **ТОЧНО** совпадать с выражением в запросе!

**Не сработает:**

```sql
-- Индекс
CREATE INDEX idx_fts ON articles 
USING GIN (to_tsvector('russian', content));

-- Запрос (другая конфигурация!)
SELECT * FROM articles 
WHERE to_tsvector('english', content) @@ to_tsquery('postgres');
-- Seq Scan! Индекс не используется!
```

**Проверка использования индекса:**

```sql
EXPLAIN ANALYZE
SELECT * FROM articles 
WHERE to_tsvector('russian', content) @@ to_tsquery('postgres');

-- Должно быть:
-- Bitmap Index Scan using idx_fts
```

**Сленг:** "Индекс не подхватился" — Postgres не использует индекс (обычно из-за несовпадения выражений).

---

## Слайд 24: Хранение tsvector в таблице

**Время: 5 мин**

**Ассоциация:** Хранение tsvector — это как материализованное представление (precomputed).

Проблема предыдущего подхода: `to_tsvector('russian', content)` вычисляется **каждый раз** при поиске.

Решение: **хранить tsvector в отдельной колонке**.

```sql
-- Добавить колонку
ALTER TABLE articles ADD COLUMN fts tsvector;

-- Заполнить колонку
UPDATE articles 
SET fts = to_tsvector('russian', title || ' ' || content);

-- Создать индекс
CREATE INDEX idx_fts ON articles USING GIN (fts);

-- Поиск (быстрее!)
SELECT * FROM articles WHERE fts @@ to_tsquery('postgres');
```

**Преимущества:**
- ✅ tsvector вычисляется один раз (при INSERT/UPDATE)
- ✅ Поиск быстрее (не надо вычислять to_tsvector)
- ✅ Индекс проще (индексируем колонку, а не выражение)

**Недостатки:**
- ❌ Занимает место на диске
- ❌ Нужно обновлять при изменении данных

**Сленг:** "Заматериализовать tsvector" — сохранить в колонку.

---

## Слайд 25: Автоматическое обновление tsvector

**Время: 5 мин**

**Ассоциация:** Триггер — это как хук в React (автоматически вызывается при изменении).

Проблема: при UPDATE articles нужно не забыть обновить fts. Легко забыть!

Решение: **триггер**.

```sql
-- Триггер для автообновления
CREATE TRIGGER tsvector_update BEFORE INSERT OR UPDATE
ON articles FOR EACH ROW EXECUTE FUNCTION
tsvector_update_trigger(fts, 'pg_catalog.russian', title, content);
```

**Что происходит:**
1. При INSERT или UPDATE строки
2. Автоматически вызывается функция `tsvector_update_trigger`
3. Она обновляет колонку `fts` на основе `title` и `content`

**Теперь можно забыть про fts:**

```sql
-- Просто вставляем данные
INSERT INTO articles (title, content) 
VALUES ('PostgreSQL', 'Полнотекстовый поиск');

-- fts обновился автоматически!
SELECT * FROM articles WHERE fts @@ to_tsquery('поиск');
```

**Это как computed property в Vue.js** — обновляется автоматически при изменении зависимостей.

**Сленг:** "Повесить триггер" — создать триггер на таблицу.

---

## Слайд 26: Подсветка результатов — ts_headline

**Время: 4 мин**

**Ассоциация:** ts_headline — это как сниппеты в Google (выделяет найденные слова в контексте).

Нашли документы. Хотим показать пользователю, **где именно** нашлось слово.

```sql
SELECT 
  title,
  ts_headline('russian', content, query) AS snippet
FROM articles, to_tsquery('russian', 'postgres') query
WHERE fts @@ query;

-- Результат:
-- "...использование <b>Postgres</b> для поиска..."
```

**Что делает ts_headline:**
1. Находит в тексте найденные слова
2. Вырезает контекст вокруг них (несколько слов до и после)
3. Оборачивает найденные слова в `<b>` теги

**Параметры:**

```sql
ts_headline('russian', content, query, 
  'StartSel=<mark>, StopSel=</mark>, MaxWords=50, MinWords=20')
```

- **StartSel/StopSel** — теги для выделения (по умолчанию `<b>` и `</b>`)
- **MaxWords** — максимум слов в сниппете
- **MinWords** — минимум слов в сниппете

**Сленг:** "Хайлайтнуть результаты" — подсветить найденные слова.

---

## Слайд 27: Фразовый поиск

**Время: 4 мин**

**Ассоциация:** Фразовый поиск — это как поиск в кавычках в Google ("точная фраза").

Обычный поиск ищет слова в любом порядке. Фразовый — в определенном порядке.

```sql
-- Найти "machine learning" именно в таком порядке
SELECT * FROM articles 
WHERE fts @@ phraseto_tsquery('english', 'machine learning');
```

**phraseto_tsquery** автоматически добавляет оператор `<->` между словами.

**Эквивалентно:**

```sql
SELECT * FROM articles 
WHERE fts @@ to_tsquery('machine <-> learning');
```

**С расстоянием (до 2 слов между):**

```sql
SELECT * FROM articles 
WHERE fts @@ to_tsquery('machine <2> learning');
```

Найдет:
- "machine learning" ✅
- "machine deep learning" ✅
- "machine and deep learning" ✅
- "machine very deep learning" ❌ (3 слова между)

**Когда использовать:**
- Поиск цитат
- Поиск названий (точное совпадение)
- Поиск устойчивых выражений

**Сленг:** "Фразовик" — фразовый поиск.

---

## Слайд 28: Мультиязычный поиск

**Время: 4 мин**

**Ассоциация:** Мультиязычный поиск — это как i18n для поиска.

Проблема: у вас документы на разных языках. Как искать?

**Решение 1: Отдельные колонки**

```sql
ALTER TABLE articles ADD COLUMN fts_ru tsvector;
ALTER TABLE articles ADD COLUMN fts_en tsvector;

UPDATE articles 
SET fts_ru = to_tsvector('russian', content),
    fts_en = to_tsvector('english', content);

-- Поиск по русскому
SELECT * FROM articles WHERE fts_ru @@ to_tsquery('russian', 'поиск');

-- Поиск по английскому
SELECT * FROM articles WHERE fts_en @@ to_tsquery('english', 'search');
```

**Решение 2: Объединенный индекс**

```sql
CREATE INDEX idx_multi ON articles USING GIN (
  to_tsvector('russian', title) || 
  to_tsvector('english', title)
);

-- Поиск по обоим языкам
SELECT * FROM articles 
WHERE (to_tsvector('russian', title) || to_tsvector('english', title)) 
  @@ to_tsquery('search | поиск');
```

**Когда использовать:**
- Решение 1 — если нужна точность (разные языки обрабатываются по-разному)
- Решение 2 — если нужна простота (один индекс, один запрос)

**Сленг:** "Мультиязычка" — мультиязычный поиск.

---

## Слайд 29: Поиск по JSON

**Время: 5 мин**

**Ассоциация:** Поиск по JSON — это как полнотекстовый поиск по NoSQL данным.

Postgres умеет хранить JSON. Можно ли по нему искать? Да!

**Простой способ (JSON → text):**

```sql
SELECT * FROM products 
WHERE to_tsvector(metadata::text) @@ 'waterproof';
```

Превращаем JSON в текст, потом ищем. Работает, но грубо.

**Продвинутый способ (JSONB с индексом):**

```sql
CREATE INDEX ON products USING GIN (
  to_tsvector('english', 
    jsonb_path_query_array(metadata, '$.**.text')::text
  )
);
```

`jsonb_path_query_array(metadata, '$.**.text')` — извлекает все поля `text` из JSON (на любом уровне вложенности).

**Поиск по массивам:**

```sql
SELECT * FROM posts 
WHERE to_tsvector(array_to_string(tags, ' ')) @@ 'postgresql';
```

`array_to_string(tags, ' ')` — склеивает массив в строку через пробел.

**Даже числа:**

```sql
SELECT to_tsvector('simple', '192.168.1.1');
-- '192.168.1.1':1
```

**Правило:** Всё, что можно привести к строке → можно искать.

**Сленг:** "Зафуллтекстить JSON" — сделать полнотекстовый поиск по JSON.

---

## Слайд 30: Когда PostgreSQL FTS достаточно

**Время: 5 мин**

**Ассоциация:** PostgreSQL FTS vs Elasticsearch — это как монолит vs микросервисы.

**PostgreSQL FTS достаточно:**

✅ **До 10-100 млн документов**
Проверено на практике. Дальше начинаются проблемы с производительностью.

✅ **Простой поиск (не ML-ранжирование)**
Если вам не нужен "learning to rank" с нейросетями.

✅ **Нужна транзакционность**
Откатили транзакцию — откатился и поиск.

✅ **Хочется избежать "зоопарка технологий"**
Один сервис вместо двух. Один бэкап. Один мониторинг.

✅ **Бюджет на инфраструктуру ограничен**
Elasticsearch жрет память. Postgres уже есть.

**Нужен Elasticsearch/Solr:**

❌ **Миллиарды документов**
Postgres не масштабируется горизонтально для поиска.

❌ **Сложная аналитика (aggregations, facets)**
Группировки, фасеты, гистограммы — это сильная сторона Elastic.

❌ **ML-ранжирование (learning to rank)**
Обучение модели ранжирования на кликах пользователей.

❌ **Распределенный поиск (sharding)**
Поиск по 100 серверам параллельно.

❌ **Fuzzy search с высокой точностью**
Postgres умеет fuzzy, но Elastic делает это лучше.

**Итоговая мудрость:**

> "Don't use Elasticsearch just because everyone else does.  
> PostgreSQL FTS — это 80% функциональности за 20% сложности."  
> — Закон Парето для поиска

**Начните с Postgres FTS, мигрируйте на Elastic только когда упретесь в лимиты.**

Это как начинать с монолита, а не сразу с микросервисов. YAGNI (You Aren't Gonna Need It).

**Сленг:** "Не переусложняй" — не добавляй Elasticsearch, если Postgres справляется.

---

## Итоги лекции (5 мин)

**Что мы узнали:**

1. **LIKE — зло** для полнотекстового поиска (нет морфологии, нет индексов)
2. **Внешние поисковики — оверкилл** для большинства задач (синхронизация, транзакции)
3. **tsvector и tsquery** — внутреннее представление документов и запросов
4. **Анализаторы** — разбивают текст на токены с типами
5. **Словари** — нормализуют токены (стемминг, синонимы, стоп-слова)
6. **Конфигурации** — связывают анализаторы и словари
7. **Индексы** — GIN для read-heavy, GiST для write-heavy, RUM для сортировки
8. **Практика** — хранить tsvector в колонке, триггеры для автообновления

**Домашнее задание:**

1. Создайте таблицу с полнотекстовым поиском
2. Добавьте GIN индекс
3. Попробуйте фразовый поиск
4. Поэкспериментируйте с весами лексем

**Вопросы?**

---

**Общее время лекции: ~90 минут**

**Распределение времени:**
- Введение и проблемы (13 мин)
- Документы и запросы (19 мин)
- Анализаторы и словари (27 мин)
- Индексы (17 мин)
- Практика (19 мин)
- Итоги (5 мин)
