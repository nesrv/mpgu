Отличный выбор — **сценарий 3** очень наглядный и обычно «щёлкает» у студентов 💡
Ниже — **подробный сценарий занятия** (можно проводить как одно 90-минутное или два по 45).

---

# Сценарий занятия 3

## Масштабирование сервисов в Docker Compose

### 🎯 Цели занятия

Студент должен:

* понять, что такое **горизонтальное масштабирование**
* уметь масштабировать сервис в Docker Compose
* увидеть ограничения Docker Compose как инструмента оркестрации
* осознать необходимость балансировки нагрузки

---

## 🧠 Теоретическая часть (20 минут)

### 1. Что такое масштабирование

Обсуждение с примерами:

* вертикальное (больше CPU / RAM)
* горизонтальное (больше экземпляров сервиса)

👉 акцент: Docker Compose = **локальная оркестрация**, не продакшен-решение

---

### 2. Как Docker Compose масштабирует сервис

Ключевые идеи:

* каждый сервис → контейнер
* при scale → несколько контейнеров одного сервиса
* **один образ, один конфиг, разные контейнеры**
* каждому контейнеру присваивается:

  * уникальный container id
  * hostname

Команда:

```bash
docker compose up --scale backend=3
```

Важно проговорить:

* в `docker-compose.yml` **не пишется** количество реплик
* масштабирование — через CLI

---

### 3. Ограничения Docker Compose

Сразу честно:

* ❌ нет встроенного load balancer
* ❌ нет auto-scaling
* ❌ нет service discovery как в k8s
* ✅ отлично подходит для обучения и локальной разработки

---

## 🧪 Практическая часть (50 минут)

### Шаг 1. Подготовка проекта (10 минут)

Структура:

```text
project/
├── docker-compose.yml
└── app/
    ├── app.py
    └── Dockerfile
```

---

### Шаг 2. Backend-приложение (15 минут)

**app.py (Python + Flask):**

```python
from flask import Flask
import socket

app = Flask(__name__)

@app.route("/")
def hello():
    return f"Hello from container: {socket.gethostname()}\n"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
```

**Dockerfile:**

```dockerfile
FROM python:3.11-slim

WORKDIR /app
COPY app.py .

RUN pip install flask

CMD ["python", "app.py"]
```

---

### Шаг 3. Docker Compose конфиг (10 минут)

```yaml
version: "3.9"

services:
  backend:
    build: ./app
    ports:
      - "5000"
```

⚠️ Обратить внимание студентов:

* порт **не фиксированный** (`"5000"`, а не `"5000:5000"`)
* Docker сам выберет внешний порт

---

### Шаг 4. Запуск и масштабирование (15 минут)

1. Запуск одного контейнера:

```bash
docker compose up --build
```

2. Проверка:

```bash
curl localhost:<port>
```

3. Остановка (`Ctrl+C`)

4. Запуск с масштабированием:

```bash
docker compose up --build --scale backend=3
```

5. Проверка:

```bash
docker compose ps
```

Пример вывода:

```text
backend-1
backend-2
backend-3
```

6. Запросы:

```bash
curl localhost:<port>
```

👉 контейнер **всегда один и тот же**

---

## 🔍 Аналитическая часть (15 минут)

### Вопрос студентам:

> Мы запустили 3 контейнера. Почему нагрузка не распределяется?

Ожидаемые ответы:

* нет балансировщика
* порт проброшен только к одному контейнеру
* Docker Compose не управляет трафиком

---

### Демонстрация проблемы

```bash
docker compose logs backend
```

Показываем:

* запросы идут только в один контейнер

---

## 🧠 Ключевые выводы (10 минут)

Студенты формулируют сами, преподаватель фиксирует:

* масштабирование ≠ балансировка
* Docker Compose умеет **создавать реплики**
* но **не умеет управлять трафиком**
* для реальной нагрузки нужен:

  * Nginx
  * Traefik
  * Kubernetes

---

## 🧪 Дополнительные задания (по уровню)

### Лёгкое

* добавить вывод `container id`
* изменить текст ответа сервиса

### Среднее

* добавить второй endpoint `/health`
* посмотреть, как ведут себя реплики

### Сложное (звёздочка ⭐)

* добавить Nginx
* распределить запросы между репликами (подводка к занятию 4)

---

## 📝 Домашнее задание

**Вариант 1:**
Ответить письменно:

> Почему масштабирование без балансировщика не даёт выигрыша?

**Вариант 2 (практика):**

* масштабировать backend до 5 реплик
* зафиксировать наблюдения в README

---

## 👨‍🏫 Методические подсказки

* специально дайте студентам «ожидание магии»
* пусть удивятся, что ничего не работает 😄
* после этого занятие про Nginx зайдёт идеально

---

Если хочешь, в следующем шаге могу:

* оформить это как **лабораторную работу**
* сделать **PDF/методичку**
* или сразу **подробный сценарий занятия 4 (Nginx + load balancing)**
