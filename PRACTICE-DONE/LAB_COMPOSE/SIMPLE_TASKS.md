# 📚 Простые задания по Docker Compose

## Задание 1: Запустить nginx
**Что делаем**: Запускаем готовый nginx сервис

### Шаг 1: Посмотреть docker-compose.yml
```yaml
services:
  app:
    build: .
    ports:
      - "8000:8000"
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
```

### Шаг 2: Запустить nginx
```bash
docker-compose up nginx
```

### Шаг 3: Проверить
Открыть http://localhost - должна появиться страница nginx

**Что изучили**: запуск отдельных сервисов, проброс портов

---

## Задание 2: Изменить порт приложения
**Что делаем**: Меняем порт с 8000 на 3000

### Шаг 1: В docker-compose.yml найти:
```yaml
app:
  ports:
    - "8000:8000"
```

### Шаг 2: Изменить на:
```yaml
app:
  ports:
    - "3000:8000"
```

### Шаг 3: Перезапустить
```bash
# Убедитесь что находитесь в папке LAB_COMPOSE
cd LAB_COMPOSE
docker-compose down
docker-compose up app -d
```

### Шаг 4: Проверить
```bash
# Проверить что контейнер запущен
docker-compose ps
# Открыть http://localhost:3000/docs
# Для Windows PowerShell:
Invoke-WebRequest -Uri http://localhost:3000/hit -Method POST
# Или просто открыть http://localhost:3000/docs в браузере
```

**Что изучили**: проброс портов host:container

---

## Задание 3: Добавить переменную окружения
**Что делаем**: Добавляем переменную DEBUG=true

### Шаг 1: В docker-compose.yml в сервисе app добавить:
```yaml
app:
  environment:
    DEBUG: "true"
```

### Шаг 2: Пересобрать и запустить
```bash
# Убедитесь что находитесь в папке LAB_COMPOSE
cd LAB_COMPOSE
docker-compose down
docker-compose up --build app -d
```

### Шаг 3: Проверить
```bash
# Проверить статус
docker-compose ps
# Открыть http://localhost:8000/debug
# Должно показать "debug": "true"
```

**Что изучили**: переменные окружения, пересборка

---

## Задание 4: Добавить volume для данных
**Что делаем**: Сохраняем данные при перезапуске

### Шаг 1: В docker-compose.yml добавить в конец:
```yaml
volumes:
  app_data:
```

### Шаг 2: В сервисе app добавить:
```yaml
app:
  build: .
  ports:
    - "8000:8000"
  volumes:
    - app_data:/app/data
```

### Шаг 3: Перезапустить
```bash
docker-compose down
docker-compose up -d
```

### Шаг 4: Проверить
```bash
docker volume ls
```

**Что изучили**: volumes, сохранение данных

---

## Задание 5: Создать .env файл
**Что делаем**: Выносим переменные в отдельный файл

### Шаг 1: Создать файл .env:
```
DEBUG=true
APP_NAME=MyApp
PORT=8000
```

### Шаг 2: В docker-compose.yml изменить app:
```yaml
app:
  env_file:
    - .env
```

### Шаг 3: Перезапустить
```bash
docker-compose up --build
```

**Что изучили**: .env файлы, env_file

---

## Задание 6: Добавить healthcheck
**Что делаем**: Проверяем здоровье сервисов

### Шаг 1: Установить curl в Dockerfile:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

RUN pip install fastapi uvicorn
RUN apt-get update && apt-get install -y curl

COPY main.py .

CMD ["python", "main.py"]
```

### Шаг 2: В сервисе app добавить:
```yaml
app:
  build: .
  ports:
    - "8000:8000"
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    interval: 30s
    timeout: 10s
    retries: 3
```

### Шаг 2: Запустить
```bash
docker-compose up --build
```

### Шаг 3: Проверить статус
```bash
docker-compose ps
```

**Как работает healthcheck**:
- `test` - команда для проверки (возвращает 0 = здоров)
- `interval: 30s` - проверять каждые 30 секунд
- `timeout: 10s` - ждать ответ максимум 10 секунд
- `retries: 3` - 3 неудачные попытки = контейнер нездоров

**Для чего нужен**:
- Автоматический перезапуск при сбоях
- Мониторинг состояния приложения
- Оркестраторы (например, Kubernetes) используют эту информацию

**Что изучили**: healthcheck, мониторинг сервисов

---

## Задание 7: Запустить несколько копий приложения
**Что делаем**: Масштабируем app сервис

### Шаг 1: Убрать фиксированный порт у app:
```yaml
app:
  ports:
    - "8000-8002:8000"
```

### Шаг 2: Запустить 3 копии
```bash
docker-compose up --scale app=3 -d
```

### Шаг 3: Проверить
```bash
docker-compose ps
# Проверяем разные порты (для Windows PowerShell):
Invoke-WebRequest -Uri http://localhost:8000/hit -Method POST
Invoke-WebRequest -Uri http://localhost:8001/hit -Method POST
Invoke-WebRequest -Uri http://localhost:8002/hit -Method POST
```

**Что изучили**: масштабирование, диапазон портов

---

## Задание 8: Добавить nginx как балансировщик
**Что делаем**: nginx распределяет запросы между app копиями

### Шаг 1: Создать файл nginx.conf в папке LAB_COMPOSE:
```nginx
upstream app {
    server app:8000;
}

server {
    listen 80;
    location / {
        proxy_pass http://app;
    }
}
```

**Объяснение файла**:
- `upstream app` - группа серверов под именем "app"
- `server app:8000` - адрес нашего FastAPI приложения
- `listen 80` - nginx слушает порт 80
- `location /` - все запросы (/hit, /docs, и т.д.)
- `proxy_pass` - перенаправляет на app:8000

**Поэтому**:
- http://localhost/hit → nginx:80 → app:8000/hit
- http://localhost/docs → nginx:80 → app:8000/docs

### Шаг 2: В docker-compose.yml изменить nginx:
```yaml
nginx:
  image: nginx:alpine
  ports:
    - "80:80"
  volumes:
    - ./nginx.conf:/etc/nginx/conf.d/default.conf
  depends_on:
    - app
```

### Шаг 3: Запустить
```bash
docker-compose up --scale app=2
```

### Шаг 4: Проверить
```bash
# Для Windows PowerShell:
Invoke-WebRequest -Uri http://localhost/hit -Method POST
# Или открыть http://localhost/docs и нажать Try it out
```

**Что происходит**:
- nginx получает запрос на порт 80
- Перенаправляет на один из app контейнеров
- app увеличивает счетчик в памяти
- Возвращает {"count": число}

**Что изучили**: volumes с файлами, балансировка нагрузки

---

## Проверочные команды

**ВАЖНО**: Все команды выполнять в папке LAB_COMPOSE!

```bash
# Перейти в папку проекта
cd LAB_COMPOSE

# Посмотреть все сервисы
docker-compose ps

# Посмотреть логи
docker-compose logs app

# Остановить все
docker-compose down

# Запустить все заново
docker-compose up -d

# Посмотреть volumes
docker volume ls

# Масштабирование
docker-compose up --scale app=3
```