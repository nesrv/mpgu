# Настройка проекта в WSL2

## Быстрый старт

### 1. Установка WSL2 (если еще не установлен)

В PowerShell от имени администратора:
```powershell
wsl --install
```

Перезагрузите компьютер после установки.

### 2. Запуск WSL и настройка

Откройте Ubuntu из меню Пуск или выполните:
```powershell
wsl
```

### 3. Автоматическая настройка

Выполните скрипт настройки:
```bash
cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL
bash setup_wsl.sh
```

**Примечание:** На Windows файловой системе (`/mnt/c/`) нельзя изменить права через `chmod`, поэтому выполняем скрипт напрямую через `bash`.

Или настройте вручную (см. ниже).

## Ручная настройка

### Шаг 1: Обновление системы
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y python3 python3-pip curl build-essential
```

### Шаг 2: Установка uv
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc  # или перезапустите терминал
```

### Шаг 3: Переход в проект
```bash
cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL
```

### Шаг 4: Создание виртуального окружения и установка зависимостей

**Важно:** Виртуальное окружение нужно создавать в Linux файловой системе (не на `/mnt/c/`), чтобы избежать проблем с правами доступа.

```bash
# Создание виртуального окружения в домашней директории
mkdir -p ~/.venvs
uv venv ~/.venvs/laba-graphql

# Активация виртуального окружения
source ~/.venvs/laba-graphql/bin/activate

# Переход в директорию проекта
cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL

# Установка зависимостей
uv pip install -r requirements.txt
```

## Работа с проектом

### Запуск PostgreSQL
```bash
docker-compose up -d
```

### Инициализация базы данных
```bash
python3 init_db.py
```

### Запуск FastAPI сервера
```bash
# Активация виртуального окружения
source ~/.venvs/laba-graphql/bin/activate

# Переход в директорию проекта
cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL

# Запуск сервера
uvicorn main:app --reload
```

## Полезные команды uv

```bash
# Активация виртуального окружения (обязательно перед работой!)
source ~/.venvs/laba-graphql/bin/activate

# Переход в директорию проекта
cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL

# Установка зависимостей
uv pip install -r requirements.txt

# Установка конкретного пакета
uv pip install fastapi

# Список установленных пакетов
uv pip list

# Обновление пакетов
uv pip install --upgrade -r requirements.txt

# Создание нового виртуального окружения
uv venv ~/.venvs/laba-graphql

# Деактивация виртуального окружения
deactivate

# Создание алиаса для быстрой активации (добавьте в ~/.bashrc)
alias activate-graphql='cd /mnt/c/W26/project/mpgu_practice/LABA-GRAPHQL && source ~/.venvs/laba-graphql/bin/activate'
```

## Преимущества uv

- ⚡ **Быстрее pip** в 10-100 раз
- 🔒 **Надежнее** - детерминированные установки
- 📦 **Меньше проблем** с зависимостями
- 🚀 **Современный** инструмент от создателей ruff

## Решение проблем

### uv не найден после установки
```bash
export PATH="$HOME/.cargo/bin:$PATH"
source ~/.bashrc
```

### Проблемы с правами доступа
```bash
sudo chown -R $USER:$USER /mnt/c/W26/project/mpgu_practice
```

### Docker не работает в WSL2
Убедитесь, что в Docker Desktop включена интеграция с WSL2:
Settings → Resources → WSL Integration → включите ваш дистрибутив

## Структура проекта

```
LABA-GRAPHQL/
├── docker-compose.yml      # Конфигурация PostgreSQL
├── sql.sql                 # SQL схема и данные
├── requirements.txt        # Python зависимости
├── init_db.py             # Скрипт инициализации БД
├── setup_wsl.sh           # Скрипт настройки WSL2
└── README_WSL.md          # Этот файл
```

