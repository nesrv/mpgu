# 🐳 Docker Compose: Оркестрация контейнеров

**Управление разработкой ПО**  


---

## Слайд 1: Заголовок

# 🐳 Docker Compose
## Оркестрация многоконтейнерных приложений

**Управление разработкой ПО**  
*30 минут | 5 частей*

---

## Слайд 2: 📋 План занятия

- 🎯 **Проблема многоконтейнерных приложений**
- 🔧 **Что такое Docker Compose**
- 📝 **Синтаксис docker-compose.yml**
- 💻 **Основные команды**
- 🚀 **Практический пример**

---

## Слайд 3: 🎯 Проблема: Сложность запуска

### Типичное веб-приложение:
```bash
# Запуск базы данных
docker run -d --name postgres \
  -e POSTGRES_DB=myapp \
  -e POSTGRES_USER=user \
  -e POSTGRES_PASSWORD=pass \
  -p 5432:5432 postgres:15

# Запуск Redis
docker run -d --name redis \
  -p 6379:6379 redis:7

# Запуск приложения
docker run -d --name webapp \
  --link postgres --link redis \
  -p 8000:8000 myapp:latest
```

**❌ Проблемы:** Много команд, сложно запомнить, ошибки в параметрах

---

## Слайд 4: ✅ Решение: Docker Compose

### Один файл, одна команда:

**docker-compose.yml:**
```yaml
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: myapp
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
  
  redis:
    image: redis:7
    ports:
      - "6379:6379"
  
  webapp:
    image: myapp:latest
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      - redis
```

**Запуск:** `docker-compose up -d`

---

## Слайд 5: 🔧 Что такое Docker Compose?

### Определение:
**Docker Compose** — инструмент для определения и запуска многоконтейнерных Docker-приложений

### Ключевые особенности:
- ✅ **Декларативный подход** — описываем что хотим, а не как
- ✅ **YAML-конфигурация** — простой и читаемый формат
- ✅ **Управление зависимостями** — автоматический порядок запуска
- ✅ **Изолированные среды** — каждый проект в своей сети
- ✅ **Масштабирование** — легко увеличить количество контейнеров

### Аналогия:
**Docker** = один музыкант  
**Docker Compose** = дирижер оркестра 🎼

---

## Слайд 6: 📝 Структура docker-compose.yml

```yaml
version: '3.8'  # Версия формата файла

services:       # Описание контейнеров
  service1:
    # конфигурация первого сервиса
  service2:
    # конфигурация второго сервиса

volumes:        # Именованные тома (опционально)
  volume1:

networks:       # Пользовательские сети (опционально)
  network1:
```

### Основные секции:
- **services** — контейнеры приложения
- **volumes** — постоянное хранение данных
- **networks** — сетевая конфигурация

---

## Слайд 7: 🏗️ Конфигурация сервиса

```yaml
services:
  webapp:
    image: python:3.11              # Готовый образ
    # ИЛИ
    build: .                        # Собрать из Dockerfile
    # ИЛИ
    build:
      context: ./app
      dockerfile: Dockerfile.prod
    
    ports:                          # Проброс портов
      - "8000:8000"
      - "8443:443"
    
    environment:                    # Переменные окружения
      - DEBUG=1
      - DATABASE_URL=postgres://...
    
    volumes:                        # Монтирование
      - ./app:/code
      - static_files:/app/static
    
    depends_on:                     # Зависимости
      - postgres
      - redis
    
    restart: unless-stopped         # Политика перезапуска
```

---

## Слайд 8: 🗄️ Работа с базами данных

```yaml
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: myapp
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret123
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    restart: always
  
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

volumes:
  postgres_data:
  redis_data:
```

**💡 Важно:** Данные сохраняются в именованных томах

---

## Слайд 9: 🌐 Сети и связи

```yaml
services:
  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
    networks:
      - frontend_net
  
  backend:
    image: myapp:latest
    networks:
      - frontend_net
      - backend_net
    depends_on:
      - database
  
  database:
    image: postgres:15
    networks:
      - backend_net
    # НЕ открываем порты наружу!

networks:
  frontend_net:
  backend_net:
```

**🔒 Безопасность:** База данных изолирована от внешнего мира

---

## Слайд 10: 💻 Основные команды

```bash
# Запуск всех сервисов
docker-compose up

# Запуск в фоновом режиме
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Остановка с удалением томов
docker-compose down -v

# Просмотр логов
docker-compose logs
docker-compose logs webapp

# Масштабирование сервиса
docker-compose up -d --scale webapp=3

# Пересборка образов
docker-compose build

# Выполнение команды в контейнере
docker-compose exec webapp bash
```

---

## Слайд 11: 🔄 Жизненный цикл проекта

```bash
# 1. Разработка
docker-compose up          # Запуск с логами в консоли

# 2. Тестирование
docker-compose -f docker-compose.test.yml up --abort-on-container-exit

# 3. Продакшн
docker-compose -f docker-compose.prod.yml up -d

# 4. Обновление
docker-compose pull        # Скачать новые образы
docker-compose up -d       # Пересоздать контейнеры

# 5. Очистка
docker-compose down -v --rmi all
```

### Разные файлы для разных сред:
- `docker-compose.yml` — разработка
- `docker-compose.prod.yml` — продакшн
- `docker-compose.test.yml` — тестирование

---

## Слайд 12: 🚀 Практический пример: Django + PostgreSQL

**docker-compose.yml:**
```yaml
version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: django_app
      POSTGRES_USER: django
      POSTGRES_PASSWORD: django123
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - DATABASE_URL=postgres://django:django123@db:5432/django_app
    depends_on:
      - db

volumes:
  postgres_data:
```

---

## Слайд 13: 🚀 Запуск примера

**Dockerfile:**
```dockerfile
FROM python:3.11-slim
WORKDIR /code
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
```

**requirements.txt:**
```
Django==4.2.0
psycopg2-binary==2.9.5
```

**Команды:**
```bash
# Создание проекта
django-admin startproject myproject .

# Запуск
docker-compose up -d

# Миграции
docker-compose exec web python manage.py migrate

# Создание суперпользователя
docker-compose exec web python manage.py createsuperuser
```

---

## Слайд 14: ⚡ Продвинутые возможности

### 1. Override файлы:
```bash
# Автоматически применяется docker-compose.override.yml
docker-compose up

# Использование нескольких файлов
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up
```

### 2. Переменные окружения:
```yaml
# .env файл
POSTGRES_PASSWORD=secret123
APP_VERSION=1.0.0

# docker-compose.yml
services:
  app:
    image: myapp:${APP_VERSION}
    environment:
      - DB_PASSWORD=${POSTGRES_PASSWORD}
```

### 3. Healthchecks:
```yaml
services:
  webapp:
    image: myapp:latest
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

---

## Слайд 15: 🎓 Резюме и лучшие практики

### ✅ Преимущества Docker Compose:
- **Простота** — один файл, одна команда
- **Воспроизводимость** — одинаково работает везде
- **Изоляция** — каждый проект в своей сети
- **Масштабируемость** — легко добавить сервисы

### 🎯 Лучшие практики:
- Используйте именованные тома для данных
- Не открывайте порты БД наружу
- Создавайте отдельные файлы для разных сред
- Используйте .env файлы для секретов
- Добавляйте healthcheck для критичных сервисов
- Указывайте конкретные версии образов

### 🚀 Следующий шаг:
**Kubernetes** для enterprise-решений с сотнями контейнеров

---

**🎉 Спасибо за внимание!**

*Docker Compose — это оркестр, где каждый контейнер играет свою партию! 🎼*

---

## Заметки лектору:

### Подготовка к лекции:
- Эта лекция является продолжением темы Docker. Убедитесь, что студенты знакомы с основами Docker (образы, контейнеры, Dockerfile)
- Подготовьте демо-среду с установленным Docker и Docker Compose
- Заранее скачайте образы: `docker pull postgres:15`, `docker pull redis:7`, `docker pull python:3.11-slim`
- Подготовьте примеры кода для демонстрации
- Время на вопросы: оставьте 5 минут в конце
- Практическое задание: студенты должны создать свой docker-compose.yml
- Общая продолжительность: 30 минут + 10 минут на вопросы
- Интерактивность: задавайте вопросы аудитории каждые 5-7 минут

### Ключевые моменты для объяснения:

**Слайд 1:** "Добро пожаловать на занятие по Docker Compose! Сегодня мы изучим, как управлять сложными приложениями, состоящими из нескольких контейнеров. Если Docker - это один музыкант, то Docker Compose - это дирижер целого оркестра."

**Слайд 3:** Покажите выполнение команд в терминале, намеренно сделайте ошибку в одной из них, чтобы показать, как легко ошибиться.

**Слайд 4:** "Это декларативный подход - мы описываем ЧТО хотим получить, а не КАК это сделать."

**Слайд 5:** "Docker Compose входит в состав Docker Desktop, так что если у вас установлен Docker, то Compose уже есть."

**Слайд 7:** "depends_on запускает контейнеры в правильном порядке, но не ждет, пока сервис будет готов принимать соединения. Для этого нужны healthcheck'и."

**Слайд 8:** "В продакшене НЕ открывайте порты БД наружу! Это нужно только для разработки и отладки."

**Слайд 9:** "Такая архитектура защищает БД от прямых атак из интернета. Даже если взломают frontend, до БД еще нужно добраться через backend."

**Слайд 10:** "Все команды нужно выполнять в директории с файлом docker-compose.yml!"

**Слайд 13:** "При первом запуске PostgreSQL инициализируется - это занимает время. Django может упасть при первом запуске, если БД еще не готова - это нормально, перезапустится автоматически."

### Типичные вопросы студентов:
- "Можно ли использовать Docker Compose в продакшене?" - Да, но для небольших проектов
- "Как обновлять приложения без простоя?" - Blue-green deployment, rolling updates
- "Что делать, если контейнер падает?" - restart policies, healthchecks, мониторинг
- "Как масштабировать?" - `--scale`, но лучше переходить на Kubernetes
- "Как бэкапить данные?" - volumes, регулярные дампы БД

### Домашнее задание:
"Создайте docker-compose.yml для стека LAMP (Linux, Apache, MySQL, PHP) или MEAN (MongoDB, Express, Angular, Node.js). Попробуйте разные комбинации сервисов."

### Дополнительные ресурсы:
- Awesome Compose: github.com/docker/awesome-compose - готовые примеры
- Docker Compose в продакшене: 12factor.net - принципы разработки
- Мониторинг: Portainer для визуального управления контейнерами