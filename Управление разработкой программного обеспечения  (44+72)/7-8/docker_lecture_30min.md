# Управление разработкой

# 🐳 Docker: Контейнеризация для разработчиков



## 📋 План лекции 

1. **Проблема и мотивация** (5 мин)
2. **От VM к контейнерам** (5 мин)
3. **Основные концепции Docker** (7 мин)
4. **Базовые команды** (8 мин)
5. **Практический пример** (5 мин)

---

## 🎯 Часть 1: Проблема "У меня работает!" (5 минут)

### Типичные проблемы разработки

**Ситуация 1: Разные окружения**

```
Разработчик: "У меня на ноутбуке работает!"
DevOps: "А на сервере падает..."
```

**Причины:**

- Разные версии Python (3.8 vs 3.11)
- Разные библиотеки (requests 2.25 vs 2.31)
- Разные ОС (Windows vs Linux)
- Разные переменные окружения

**Ситуация 2: Конфликт зависимостей**

```
Проект A: требует Django 3.2
Проект B: требует Django 4.2
Один сервер: 😱
```

### Что нужно?

✅ **Изоляция** - каждое приложение в своём окружении
✅ **Воспроизводимость** - одинаково работает везде
✅ **Портативность** - легко переносить между машинами
✅ **Эффективность** - минимум накладных расходов

**Решение: Docker! 🐳**

---

## 🔄 Часть 2: Эволюция изоляции (5 минут)

### 1️⃣ Физические серверы (2000-е)

```
Приложение A → Сервер 1 (простаивает 80% времени)
Приложение B → Сервер 2 (простаивает 70% времени)
Приложение C → Сервер 3 (простаивает 90% времени)
```

**Проблемы:**

- 💸 Дорого (много железа)
- 🐌 Неэффективно (ресурсы простаивают)
- 🔧 Сложно масштабировать

### 2️⃣ Виртуальные машины (2010-е)

```
Физический сервер
├── VM 1: Ubuntu + App A (2 GB RAM, 20 GB диск)
├── VM 2: CentOS + App B (2 GB RAM, 20 GB диск)
└── VM 3: Debian + App C (2 GB RAM, 20 GB диск)
```

**Плюсы:**

- ✅ Изоляция
- ✅ Эффективнее физических серверов

**Минусы:**

- ❌ Каждая VM = полная ОС (тяжело!)
- ❌ Долгий запуск (минуты)
- ❌ Много ресурсов (гигабайты RAM)

**Аналогия:** Каждую коробку везём в отдельном грузовике 🚚🚚🚚

### 3️⃣ Контейнеры (сейчас)

```
Хостовая ОС (Linux)
├── Container 1: App A (50 MB, 100 MB RAM)
├── Container 2: App B (80 MB, 150 MB RAM)
└── Container 3: App C (60 MB, 120 MB RAM)
```

**Преимущества:**

- ✅ Легковесные (мегабайты вместо гигабайт)
- ✅ Быстрый запуск (секунды)
- ✅ Общее ядро ОС (эффективность)
- ✅ Изоляция процессов

**Аналогия:** Все коробки на одном корабле в стандартных контейнерах 🚢📦📦📦

---

## 🧩 Часть 3: Основные концепции Docker (7 минут)

### 1. Docker Image (Образ) 📦

**Что это?** Шаблон, "снимок" приложения со всеми зависимостями.

**Аналогия:** Образ = ISO-файл операционной системы или класс в ООП

**Состав образа:**

- Базовая ОС (обычно минимальная Linux)
- Приложение
- Библиотеки и зависимости
- Конфигурация

**Пример:**

```
python:3.11-slim (базовый образ)
├── Python 3.11
├── pip
└── минимальная Linux
```

### 2. Docker Container (Контейнер) 🏃

**Что это?** Запущенный экземпляр образа.

**Аналогия:** Контейнер = запущенная программа или объект класса

```
Image (класс)          Container (объект)
python:3.11     →      running_app_1
                →      running_app_2
                →      running_app_3
```

**Свойства контейнера:**

- Изолированная файловая система
- Собственная сеть
- Ограниченные ресурсы (CPU, RAM)
- Эфемерный (удалили = данные потеряны*)

*если не использовать volumes

### 3. Dockerfile 📝

**Что это?** Рецепт для создания образа.

**Структура:**

```dockerfile
# Базовый образ
FROM python:3.11-slim

# Рабочая директория
WORKDIR /app

# Копируем зависимости
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install -r requirements.txt

# Копируем код
COPY . .

# Команда запуска
CMD ["python", "app.py"]
```

**Аналогия:** Dockerfile = кулинарный рецепт 👨‍🍳

### 4. Docker Registry (Реестр) 🏪

**Что это?** Хранилище образов (как GitHub для кода).

**Docker Hub** - публичный реестр:

- `python:3.11` - официальный образ Python
- `nginx:latest` - веб-сервер Nginx
- `postgres:15` - база данных PostgreSQL

---

## 💻 Часть 4: Базовые команды (8 минут)

### Работа с образами

```bash
# Скачать образ
docker pull python:3.11

# Список локальных образов
docker images

# Удалить образ
docker rmi python:3.11

# Собрать свой образ
docker build -t myapp:1.0 .
```

### Работа с контейнерами

```bash
# Запустить контейнер
docker run python:3.11 python --version

# Запустить в фоне с именем
docker run -d --name myapp python:3.11 python app.py

# Список запущенных контейнеров
docker ps

# Список всех контейнеров (включая остановленные)
docker ps -a

# Остановить контейнер
docker stop myapp

# Удалить контейнер
docker rm myapp

# Посмотреть логи
docker logs myapp

# Зайти внутрь контейнера
docker exec -it myapp bash
```

### Полезные флаги

```bash
# Пробросить порты (хост:контейнер)
docker run -p 8080:80 nginx

# Примонтировать директорию
docker run -v /host/path:/container/path myapp

# Переменные окружения
docker run -e DATABASE_URL=postgres://... myapp

# Автоудаление после остановки
docker run --rm myapp

# Интерактивный режим
docker run -it python:3.11 bash
```

---

## 🚀 Часть 5: Практический пример (5 минут)

### Задача: Запустить простое Python приложение

**1. Создаём приложение**

`app.py`:

```python
from fastapi import FastAPI
import uvicorn

app = FastAPI()

@app.get("/")
def hello():
    return {"message": "Hello from Docker! 🐳"}

if __name__ == '__main__':
    uvicorn.run(app, host='0.0.0.0', port=8000)
```

`requirements.txt`:

```
fastapi==0.109.0
uvicorn==0.27.0
```

**2. Создаём Dockerfile**

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app.py .

EXPOSE 8000

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
```

**3. Собираем образ**

```bash
docker build -t fastapi-app:1.0 .
```

**Что происходит:**

1. Скачивается базовый образ Python
2. Создаётся рабочая директория
3. Устанавливаются зависимости
4. Копируется код
5. Создаётся финальный образ

**4. Запускаем контейнер**

```bash
docker run -d -p 8000:8000 --name my-fastapi-app fastapi-app:1.0
```

**5. Проверяем**

```bash
curl http://localhost:8000
# {"message":"Hello from Docker! 🐳"}

# Автоматическая документация
curl http://localhost:8000/docs
```

**6. Смотрим логи**

```bash
docker logs my-fastapi-app
```

**7. Останавливаем и удаляем**

```bash
docker stop my-fastapi-app
docker rm my-fastapi-app
```

---

## 🎓 Резюме: Ключевые выводы

### Что такое Docker?

**Docker** - платформа для разработки, доставки и запуска приложений в контейнерах.

### Основные концепции

| Концепция   | Описание                  | Аналогия                  |
| -------------------- | --------------------------------- | --------------------------------- |
| **Image**      | Шаблон приложения | Класс в ООП              |
| **Container**  | Запущенный образ   | Объект класса         |
| **Dockerfile** | Рецепт образа         | Кулинарный рецепт |
| **Registry**   | Хранилище образов | GitHub для образов      |

### Преимущества Docker

✅ **Изоляция** - каждое приложение в своём окружении
✅ **Портативность** - "собрал один раз, запускай везде"
✅ **Воспроизводимость** - одинаково работает на всех машинах
✅ **Эффективность** - легковесные, быстрый запуск
✅ **Масштабируемость** - легко запустить 100 копий
✅ **Версионирование** - образы с тегами (v1.0, v2.0)

### Когда использовать Docker?

✅ Микросервисная архитектура
✅ CI/CD пайплайны
✅ Разработка в команде (одинаковое окружение)
✅ Тестирование разных версий
✅ Изоляция зависимостей
✅ Быстрое развёртывание

---

## 🚀 Продвинутые возможности Docker

### 1️⃣ Docker Compose - управление несколькими контейнерами

**Проблема:** Приложение = FastAPI + PostgreSQL + Redis. Запускать 3 контейнера вручную неудобно!

**Решение:** Docker Compose - описываем всю инфраструктуру в одном файле.

`docker-compose.yml`:

```yaml
version: '3.8'

services:
  # FastAPI приложение
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/mydb
      - REDIS_URL=redis://cache:6379
    depends_on:
      - db
      - cache

  # PostgreSQL база данных
  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=mydb
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Redis кэш
  cache:
    image: redis:7-alpine

volumes:
  postgres_data:
```

**Команды:**

```bash
# Запустить всё одной командой
docker-compose up -d

# Остановить всё
docker-compose down

# Посмотреть логи
docker-compose logs -f api

# Перезапустить сервис
docker-compose restart api
```

**Преимущества:**

- ✅ Одна команда для всей инфраструктуры
- ✅ Автоматическая сеть между контейнерами
- ✅ Управление зависимостями (depends_on)
- ✅ Легко масштабировать: `docker-compose up --scale api=3`

---

### 2️⃣ Docker Volumes - постоянное хранение данных

**Проблема:** Контейнер удалили → данные потеряны! 😱

**Решение:** Volumes - постоянное хранилище вне контейнера.

**Типы хранения:**

```
1. Volume (управляется Docker)
   docker run -v mydata:/app/data myapp
   
2. Bind mount (папка хоста)
   docker run -v /host/path:/app/data myapp
   
3. tmpfs (в памяти, временно)
   docker run --tmpfs /app/temp myapp
```

**Практический пример:**

```bash
# Создать volume
docker volume create postgres_data

# Использовать volume
docker run -d \
  -v postgres_data:/var/lib/postgresql/data \
  postgres:15

# Список volumes
docker volume ls

# Информация о volume
docker volume inspect postgres_data

# Удалить неиспользуемые volumes
docker volume prune
```

**Bind mount для разработки:**

```bash
# Код на хосте автоматически обновляется в контейнере
docker run -d \
  -v $(pwd):/app \
  -p 8000:8000 \
  fastapi-app
```

**Когда использовать:**

- 📊 **Volume:** Базы данных, постоянные данные
- 📁 **Bind mount:** Разработка, конфигурация
- 💾 **tmpfs:** Временные файлы, кэш

---

### 3️⃣ Docker Networks - сетевое взаимодействие

**Проблема:** Как контейнеры общаются друг с другом?

**Решение:** Docker Networks - виртуальные сети для контейнеров.

**Типы сетей:**

```
1. bridge (по умолчанию)
   - Изолированная сеть на хосте
   - Контейнеры общаются по IP

2. host
   - Использует сеть хоста напрямую
   - Нет изоляции портов

3. none
   - Без сети (полная изоляция)
```

**Практический пример:**

```bash
# Создать сеть
docker network create myapp-network

# Запустить контейнеры в одной сети
docker run -d \
  --name db \
  --network myapp-network \
  postgres:15

docker run -d \
  --name api \
  --network myapp-network \
  -p 8000:8000 \
  -e DATABASE_URL=postgresql://user:pass@db:5432/mydb \
  fastapi-app

# Теперь api может обращаться к db по имени!
```

**Важно:** В одной сети контейнеры видят друг друга по имени (DNS)!

```python
# В коде FastAPI можно писать:
DATABASE_URL = "postgresql://user:pass@db:5432/mydb"
#                                        ↑
#                                  имя контейнера!
```

**Команды:**

```bash
# Список сетей
docker network ls

# Информация о сети
docker network inspect myapp-network

# Подключить контейнер к сети
docker network connect myapp-network my-container

# Отключить от сети
docker network disconnect myapp-network my-container
```

---

### 4️⃣ Multi-stage builds - оптимизация размера

**Проблема:** Образ весит 1 GB, а нужно только 100 MB! 😤

**Решение:** Multi-stage builds - собираем в одном образе, запускаем в другом.

**Пример: FastAPI приложение**

**Плохо (один stage):**

```dockerfile
FROM python:3.11
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "app:app", "--host", "0.0.0.0"]

# Размер: ~1 GB (полный Python + pip + кэш)
```

**Хорошо (multi-stage):**

```dockerfile
# Stage 1: Сборка зависимостей
FROM python:3.11 AS builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user --no-cache-dir -r requirements.txt

# Stage 2: Финальный образ
FROM python:3.11-slim
WORKDIR /app

# Копируем только установленные пакеты
COPY --from=builder /root/.local /root/.local
COPY . .

# Добавляем в PATH
ENV PATH=/root/.local/bin:$PATH

CMD ["uvicorn", "app:app", "--host", "0.0.0.0"]

# Размер: ~200 MB (slim + только нужное)
```

**Пример: Go приложение**

```dockerfile
# Stage 1: Компиляция
FROM golang:1.21 AS builder
WORKDIR /app
COPY . .
RUN go build -o myapp

# Stage 2: Минимальный образ
FROM alpine:3.18
WORKDIR /app
COPY --from=builder /app/myapp .
CMD ["./myapp"]

# Размер: ~10 MB (вместо 800 MB!)
```

**Преимущества:**

- ✅ Маленький размер образа
- ✅ Быстрая загрузка/выгрузка
- ✅ Меньше уязвимостей (меньше пакетов)
- ✅ Быстрый запуск контейнера

---

### 5️⃣ Оркестрация: Docker Swarm vs Kubernetes

**Проблема:** 100 контейнеров на 10 серверах. Как управлять? 🤯

**Решение:** Оркестраторы - автоматизация управления контейнерами.

#### Docker Swarm (простой)

**Что умеет:**

- Кластер из нескольких серверов
- Автоматическое распределение контейнеров
- Балансировка нагрузки
- Самовосстановление (контейнер упал → запустить новый)

**Пример:**

```bash
# Инициализировать Swarm
docker swarm init

# Развернуть сервис (3 реплики)
docker service create \
  --name api \
  --replicas 3 \
  --publish 8000:8000 \
  fastapi-app:1.0

# Масштабировать
docker service scale api=10

# Обновить без даунтайма
docker service update --image fastapi-app:2.0 api
```

**Плюсы:**

- ✅ Простой (встроен в Docker)
- ✅ Легко начать
- ✅ Подходит для малых/средних проектов

**Минусы:**

- ❌ Меньше возможностей чем K8s
- ❌ Меньше экосистема

#### Kubernetes (мощный)

**Что умеет:**

- Всё что Swarm + намного больше
- Автомасштабирование (по CPU/памяти)
- Rolling updates, canary deployments
- Service mesh, мониторинг
- Огромная экосистема

**Пример (упрощённый):**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
    spec:
      containers:
      - name: api
        image: fastapi-app:1.0
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: "1"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  selector:
    app: fastapi
  ports:
  - port: 80
    targetPort: 8000
  type: LoadBalancer
```

**Команды:**

```bash
# Применить конфигурацию
kubectl apply -f deployment.yaml

# Масштабировать
kubectl scale deployment fastapi-app --replicas=10

# Обновить образ
kubectl set image deployment/fastapi-app api=fastapi-app:2.0

# Статус
kubectl get pods
```

**Плюсы:**

- ✅ Индустриальный стандарт
- ✅ Огромные возможности
- ✅ Подходит для любого масштаба
- ✅ Большое комьюнити

**Минусы:**

- ❌ Сложный (крутая кривая обучения)
- ❌ Требует больше ресурсов
- ❌ Overkill для малых проектов

#### Когда что использовать?

| Проект                                            | Решение |
| ------------------------------------------------------- | -------------- |
| 1-5 контейнеров                              | Docker Compose |
| 5-50 контейнеров, 1-3 сервера         | Docker Swarm   |
| 50+ контейнеров, много серверов | Kubernetes     |
| Enterprise, микросервисы                    | Kubernetes     |

---

## 📚 Что дальше?

### Дополнительные темы для изучения:

1. **Docker Security** - безопасность контейнеров
2. **Docker Registry** - свой реестр образов
3. **CI/CD с Docker** - автоматизация сборки и деплоя
4. **Мониторинг** - Prometheus, Grafana для контейнеров
5. **Логирование** - централизованный сбор логов

### Полезные ресурсы:

- 📖 Docker: https://docs.docker.com
- 🐙 Docker Compose: https://docs.docker.com/compose
- ☸️ Kubernetes: https://kubernetes.io/docs
- 🎓 Docker Hub: https://hub.docker.com
- 💡 Play with Docker: https://labs.play-with-docker.com
- 🎮 Play with Kubernetes: https://labs.play-with-k8s.com

---

## ❓ Частые вопросы

**Q: Docker работает только на Linux?**
A: Нет! Docker Desktop работает на Windows и macOS (использует виртуализацию).

**Q: Контейнер = виртуальная машина?**
A: Нет! Контейнер легковеснее, использует ядро хостовой ОС.

**Q: Данные в контейнере сохраняются?**
A: Нет, если не использовать volumes. Контейнеры эфемерные.

**Q: Можно ли запустить Windows-приложение в Docker?**
A: Да, но нужны Windows-контейнеры (работают только на Windows Server).

**Q: Docker бесплатный?**
A: Да, Docker Engine бесплатный. Docker Desktop бесплатен для личного использования.

---

## 🎯 Практическое задание

**Задача:** Создайте Docker-образ для вашего проекта

1. Напишите Dockerfile
2. Соберите образ
3. Запустите контейнер
4. Проверьте работу
5. Опубликуйте образ на Docker Hub (опционально)

**Критерии успеха:**

- ✅ Образ собирается без ошибок
- ✅ Контейнер запускается
- ✅ Приложение работает корректно
- ✅ Размер образа оптимизирован

---

## 🎉 Спасибо за внимание!

**Помните:** Docker - это не магия, это просто умная изоляция процессов! 🐳

**Главное правило:** Если работает в контейнере - будет работать везде! 🚀
